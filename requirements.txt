# ═══════════════════════════════════════
# requirements.txt
# ═══════════════════════════════════════
fastapi==0.115.0
uvicorn[standard]==0.30.0
anthropic==0.40.0
python-multipart==0.0.9
python-docx==1.1.2
openpyxl==3.1.5
PyMuPDF==1.24.14


# ═══════════════════════════════════════
# TEST EN LOCAL
# ═══════════════════════════════════════

# Créer un .env
echo "ANTHROPIC_API_KEY=sk-ant-XXXXXXXX" > .env

# Installer les dépendances
pip install -r requirements.txt

# Lancer
python main.py
# → http://localhost:8000


# ═══════════════════════════════════════
# APPEL API (curl test)
# ═══════════════════════════════════════

curl -X POST http://localhost:8000/api/analyze \
  -F "files=@mon_dce.pdf" \
  -F "mode=auto" \
  -F "output=both"

# Avec un ZIP :
curl -X POST http://localhost:8000/api/analyze \
  -F "files=@dossier_dce.zip" \
  -F "mode=auto" \
  -F "output=excel"
